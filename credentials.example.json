{
  "_comment": "Copy to %APPDATA%\\Hisho\\credentials.json and fill in. NEVER commit the real file. Env var SLACK_USER_TOKEN overrides this. Missing token disables the Slack pull.",
  "slackUserToken": "xoxp-...  (user token, scopes: search:read)"
}
